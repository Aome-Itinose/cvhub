name: infra-changed

on:
  pull_request:
    paths:
      - 'infra/**'

jobs:
  check-workflows:
    runs-on: ubuntu-24.04
    steps:
      - name: Validate workflow
        run: echo "will be realized soon ;)"

  completed:
    runs-on: ubuntu-24.04
    needs: [ check-workflows ]
    steps:
      - name: All jobs completed successfully
        run: echo "✅ All jobs finished successfully!"

